# URL base da API (use /api para proxy via Vite/Nginx em Docker)
VITE_API_URL=/api

# Target do proxy Vite em desenvolvimento (nome do servico Docker do backend)
VITE_PROXY_TARGET=http://backend:3000

# Porta exposta do frontend
FRONTEND_PORT=5173

# Porta do backend
BACKEND_PORT=3000
